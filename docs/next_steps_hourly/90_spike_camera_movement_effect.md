# Optional Spike - Camera movement effect

Goal (1 hour):
Prototype a `CameraMovementEffect` (MatrixTransformation) and confirm it compiles.

Scope:
- Create the class from the prompt.
- Do not integrate into the main pipeline yet.
- Add a small test hook (feature flag) if needed.

Push:
- Message suggestion: "spike camera movement effect (not wired)"

