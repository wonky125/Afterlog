# Optional Spike - Noir filter

Goal (1 hour):
Prototype a custom GlEffect with vignette + noise shader.

Scope:
- Add the GLSL fragment shader string.
- Add a Kotlin wrapper class.
- Verify Transformer can accept the effect.

Push:
- Message suggestion: "spike noir filter shader (not wired)"

