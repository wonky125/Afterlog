# Optional Spike - Freeze frame

Goal (1 hour):
Prototype a freeze-frame sequence using Media3 Composition.

Scope:
- Build a proof-of-concept sequence (normal -> freeze -> resume).
- Keep the change isolated and not enabled by default.

Push:
- Message suggestion: "spike freeze-frame sequence (not wired)"

