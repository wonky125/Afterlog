# Optional Spike - Audio ducking

Goal (1 hour):
Prototype volume automation for BGM under TTS.

Scope:
- Add a helper that builds a volume ramp based on TTS timestamps.
- Verify the API compiles with Media3 Transformer.
- Do not wire into production yet.

Push:
- Message suggestion: "spike audio ducking helper (not wired)"

