# Hour 7 - Device smoke test and notes

Goal:
Run a quick device pass after the critical fixes.

Steps:
- Record > 60s, confirm `VIDEO_CHUNK` and highlight logs.
- Trigger a scream event, confirm highlight clips are saved.
- Generate replay, confirm audio + video playback.
- If subtitles enabled, confirm captions are visible and on-screen.
- Rotate device mid-session (if allowed) and verify playback orientation.

Notes (fill in after test):
- Device:
- Android version:
- Results:

Push:
- Optional: "add smoke test notes"

